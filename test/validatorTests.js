var should = require('should');
var Validator = require('../lib/openrtb2_3/validator');

describe('The Validator should', function() {
	
	before(function(){
		this.validator = new Validator();
	});

	it('should')

};